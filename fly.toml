# fly.toml - ajuste app name depois do primeiro deploy ou via `fly launch`
app = "my-fly-rotator-app" 
primary_region = "iad"

[build]
  image = "fly-io-placeholder" # pode ficar vazio inicialmente se usar fly launch

[env]
  PORT = "8080"

[experimental]
  allowed_public_ports = []
